---
- name: Add node
  bigip_node:
      server: "{{ server }}"
      user: "{{ f5user }}"
      password: "{{ f5password }}"
      state: "present"
      partition: "Common"
      host: "{{ host_ip }}"
      name: "{{ member_name }}"
      monitors:
        - /Common/icmp