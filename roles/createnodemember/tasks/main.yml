---
- name: Add node
  bigip_node:
      server: "{{ server }}"
      user: "{{ user }}"
      password: "{{ password }}"
      state: "present"
      partition: "Common"
      host: "{{ host_ip }}"
      name: "{{ member_name }}"
      monitors:
        - /Common/icmp