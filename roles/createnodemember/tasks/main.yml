---
- name: Add node
  bigip_node:
      server: "{{ server }}"
      user: "{{ F5_USERNAME }}"
      password: "{{ F5_PASSWORD }}"
      state: "present"
      partition: "Common"
      host: "{{ host_ip }}"
      name: "{{ member_name }}"
      monitors:
        - /Common/icmp