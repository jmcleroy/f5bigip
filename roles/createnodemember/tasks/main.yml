---
- name: Add node
  bigip_node:
    user: "{{ F5_USER }}"
    password: "{{ F5_PASSWORD }}"
    server: "{{ inventory_hostname }}"
    validate_certs: no
    state: "present"
    partition: "Common"
    host: "{{ host_ip }}"
    name: "{{ member_name }}"
    monitors:
      - /Common/icmp